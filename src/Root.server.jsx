import React from "react";

export default function Root({ selectedId, isEditing, searchText }) {
  console.log({ selectedId, isEditing, searchText });
  return <div>Hi There from react app SERVER!</div>;
}
